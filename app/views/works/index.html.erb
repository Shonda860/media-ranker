<h2>List of Works</h2>

<% Work.by_category.each do |category_name,works|%>
<h3><%= category_name %></h3>
<%works.each do |work|%>




 <table class="table table-hover">
      <thead>
        <tr>
          <th>Votes</th>
          <th>Catergory</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Publication Year</th>
          <th>Edit</th>
        </tr>
      </thead>
      
      <tbody>

  <% if works.length > 0 %>
      <% works.each do |work| %>
      <tr>
        <td><%= work.title %></td>
        <td><%= work.category %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= work.title %></td>
         <td><%= work.title %></td>

      </tr> 
      <% end %> 
       </body>
      </table>
    <% else %>
    <h2>No Works in the system</h2>
  <% end %>
  <% end %>
  <% end %>

